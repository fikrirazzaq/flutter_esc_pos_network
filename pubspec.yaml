name: flutter_esc_pos_network
description: The library allows printing receipts using ESC/POS commands using thermal Wi-Fi/Ethernet printer.
version: 0.0.3
homepage: https://github.com/mjafartp/flutter_esc_pos_network

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_esc_pos_utils:
    git:
      url: https://github.com/fikrirazzaq/flutter_esc_pos_utils
      ref: 9b29326d38b32e76588436f9829436e4e06299ee

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
